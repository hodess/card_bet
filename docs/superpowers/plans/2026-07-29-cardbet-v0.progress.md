# Ledger d'exécution — plan CardBet V0 (2026-07-29)

Adaptations : pas de repo git (Romain gère git lui-même — AUCUN commit/branche par les agents).
Les "review packages" sont des concaténations des fichiers créés par la tâche.

## Progression

- Task 1 : complete (scaffold Vite+React+TS, build vert, fix identité card-bet/CardBet/lang=fr, review clean)
- Task 2 : complete (schéma+RLS+realtime, pgTAP 8/8, déviation validée : GRANT SELECT explicite car auto_expose_new_tables off ; review clean)
- Task 3 : complete (seed 40 cartes + test, pgTAP 11/11, transcription byte-for-byte, review clean)
- Task 4 : complete (create_game/join_game/get_server_time, pgTAP 21/21, review clean)
- Task 5 : complete (start_game/open_next_auction/deck_count, pgTAP 31/31, rotation vérifiée live, review clean)
- Task 6 : complete (place_bid, pgTAP 42/42, fix : ajout assertion GAME_NOT_PLAYING, review clean). Note reviewer pour T7 : close_auction DOIT verrouiller la ligne auctions ouverte (for update) avant de débiter la bankroll, pour sérialiser avec place_bid.
- Task 7 : complete (close_auction, pgTAP 57/57, protocole de verrous validé — pas de deadlock possible avec place_bid, review clean)
- Task 8 : complete (pg_cron purge-old-games, pgTAP 60/60, review clean — backend terminé)
- Task 9 : complete (game.ts + supabase.ts + types générés, vitest 4/4, build vert, review clean)
- Task 10 : complete (3 hooks, build vert, review clean — findings React plan-mandated consignés)
- Task 11 : complete (6 fichiers TSX, vitest 4/4 + build vert, E2E contrôleur : partie complète 2 sessions anonymes via RPC OK — secret tirage, no-op prématuré, auto-complétion, comptabilité exacte ; review clean). Test visuel navigateur restant à faire par Romain.

## Findings Minor reportés à la revue finale

- T1 : le template Vite actuel livre oxlint (.oxlintrc.json), pas eslint.config.js — la liste de staging du plan mentionne eslint.config.js à tort.
- T1 : le template Vite actuel ne met pas "strict": true explicitement dans tsconfig.app.json (défaut upstream) — vérifier à la revue finale que le strict effectif convient.
- T2 (plan-mandated) : 01_schema.sql ne teste le blocage d'écriture directe que sur games, pas les 5 autres tables.
- T2 (plan-mandated) : pas de FK croisée garantissant que player_id/current_bidder appartiennent bien au game_id de la même ligne (auctions, player_cards).
- T4 (plan-mandated) : le code d'erreur GAME_ALREADY_STARTED n'est pas testé dans 03_lobby.sql.
- T4 : rejoin idempotent ne met pas à jour le nickname ; EXECUTE par défaut accordé à anon (neutralisé par le check auth.uid()).
- T5 (plan-mandated) : le test du mélange vérifie count=40 mais pas le caractère aléatoire de l'ordre.
- T7 : pas de garde null sur next_card dans la boucle d'auto-complétion (inatteignable à 2 joueurs/deck 3, mais incohérent avec open_next_auction qui a NO_MORE_CARDS).
- T7 : close_auction ne vérifie pas que l'appelant est joueur de la partie (sans payoff exploitable — la garde horloge protège) ; branche 60 s jamais testée isolément.
- T10 (plan-mandated) : useGame.loadAll sans garde ignore/abort — course théorique si gameId change à la volée (inatteignable dans les flux V0 : GamePage se démonte). Pattern `let ignore = false` à envisager en durcissement.
- T10 (plan-mandated) : useCountdown ne reset pas à Infinity si deadline repasse à null ; trou théorique entre loadAll initial et confirmation de la souscription realtime.
- T11 (plan-mandated) : dep array du useEffect retry-close contient l'objet game entier → churn de l'intervalle à chaque événement realtime (no-op grâce à la garde, inefficacité cosmétique).
